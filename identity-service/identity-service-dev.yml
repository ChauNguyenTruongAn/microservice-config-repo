resilience4j:
  circuitbreaker:
    instances:
      user-service:
        sliding-window-size: 10
        failure-rate-threshold: 40 # Giả sử ta muốn đổi từ 50 xuống 40
        slow-call-rate-threshold: 50
        # ... các cấu hình khác